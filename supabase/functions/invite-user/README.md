// Note: TypeScript errors in this file are expected and can be ignored.
// This Edge Function runs in Deno runtime (not Node.js/TypeScript), 
// and VS Code doesn't have Deno type definitions installed.
// The @ts-ignore comments suppress these errors.
//
// To properly develop with Deno types in VS Code:
// 1. Install Deno VS Code extension
// 2. Enable Deno in workspace settings: "deno.enable": true
//
// The function will work correctly when deployed to Supabase Edge Functions.
